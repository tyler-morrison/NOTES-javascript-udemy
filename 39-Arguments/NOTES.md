# 'arguments' and Spread

As we know, during the Creation Phase of a new Execution Context, several items are created by default - Variable Environment, 'this', Outer Environment, and _'arguments'._ 'arguments' might not be as common in newer code, which will use spread; however, it's important to know if dealing with current frameworks.

_'arguments' contains a list of all the values of all the variables that you pass to a function._ Essentially, one keyword that contains all the parameters.
