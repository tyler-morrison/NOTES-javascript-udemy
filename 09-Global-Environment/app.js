// When you write variables and functions that are not 'lexically' within functions, they get attached to the Global Object that the Javascript engine creates for you.
var a = 'Hello World!';

function b() {};
